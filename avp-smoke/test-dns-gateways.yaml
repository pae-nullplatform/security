apiVersion: v1
kind: Pod
metadata:
  name: test-dns-gateways
  namespace: gateways
spec:
  containers:
  - name: dns-test
    image: alpine
    command: ['sh', '-c', 'sleep 3600']
  restartPolicy: Never
---